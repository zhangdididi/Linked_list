struct ListNode{
     int val;
     struct ListNode *next;
}

//通过赋值和修改地址来删除节点
void deleteNode(struct ListNode* node){
    //该删除节点的值改为下一个节点的值
    node->val = node->next->val;
    //将当前节点指向地址改为下一个节点的指向地址
    node->next = node->next->next;
}
